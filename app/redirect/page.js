import Landing from "../Landing";

export const metadata = {
  title: "Entrando no grupo — Central de Ofertas",
  robots: { index: false, follow: false },
};

export default function Page() {
  return <Landing redirect />;
}
